/**
 * Created by Arbeit on 12.04.14.
 */
jQuery(document).ready( function() {
    /*
    var options = {
        'icon':'arrow-up',
        timeout: 2000 /*height relative to time, (2000/timeout) * ScrolledToPosition
    };
   */
    //jQuery(this).jquery_backtotop(options);
});
